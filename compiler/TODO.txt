The following two lines cause a stacklc segfault. May need to invent a pramga node to fix this
#define IO_COMPLETE 0x800F0000
#pragma startup startup__

Add semantic checks
    function param number and type

Implement machine check interrupt vector. User mode machine checks trap, OS mode machine checks halt.

Allow int <--> ptr conversions, but prohibit others
    Implement cast syntax? or just default conversion?

figure out gcc generating dependencies

const optimizations

multi-D arrays
   mBaseType for multi-D arrays is not correct
   mBaseType for cArrayRef[3][2] on second index

redo test suite


allow structs separate from typedef?

static vars at global and local level

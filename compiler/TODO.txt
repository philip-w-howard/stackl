check return statements in functions

switch statements
function pointers
goto/label
ternary operator

Make a proper pre-processor
    __LINE__
    __FILE__
    #error STRING
    #warning STRING
    #define OBJ
    #define FUNC()
    #undef NAME
    #ifdef NAME
    #ifndef NAME
    #else
    #elif
    #endif
    #define EXISTING_NAME EQUIVALENT_DEF
    #if defined(NAME)
    #if EXPRESSION
    # string pasting
    ## concatenation
    #line LINE_NUM
    #line LINE_NUM SOURCE_FILE

    for expressions, undefined symbols are assumed to be zero

function return types
    validate the type of thing in return statement
    Don't force return 0 at end of functions

figure out gcc generating dependencies

const optimizations

static vars at local level

#include <string.h>
#include <system.h>
// test if/else
int main()
{
    int x;
    x = 5;
    if (x)
        printi(1);
    else
        printi(2);
    prints("\n");
    printi(3);
    prints("\n");
}

TODO

Check type and GetBaseType for carray's
ComputeOffsets doesn't seem to be invoked on carray's

string_lit as function arg

charp vars as arrays

assignment to charp vars
    array cell address
address-of operator

initialization on decls
short circuit of && and ||
full semantic checks
make opcodes an enum?
proper command line argument processing for stackl
file extension processing for stacklc
remove the requirement for /*END*/

WORKS

-ast command line argument to stacklc
gets getl geti
include files
assign cptr to string_lit
can declare char * vars and char[] 
can access individual chars in a char array
pointer arithmetic
character literals
print statements
expressions
Unary minus
++ -- <op>=
Test of all operators
if statements (including else)
while statements
for loops
rudimentary function calls
return statements
function return values
functions in expressions
function parameters
strings literals
prints

type taxonomy
    int
    char *       isPointer
    char []      isArray
    string_lit   isAbsolute

